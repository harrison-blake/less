<div class="min-h-screen bg-gray-100 flex flex-col justify-center items-center px-4">
  <div class="w-full max-w-xl bg-white rounded-2xl shadow-xl p-10 text-center">
    <h1 id="greeting" class="text-4xl font-bold text-indigo-700 mb-6">Welcome back!</h1>
    <p class="text-lg text-gray-700 mb-4">You’ve logged in <%= @user.total_logins %> times.</p>
    <p class="text-lg text-gray-600">
      Last login: <%= @user.last_logged_in_at&.strftime("%B %d, %Y %l:%M %p") || "N/A" %>
    </p>
  </div>
</div>
